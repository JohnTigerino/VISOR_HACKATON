<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- SearchView con ícono de búsqueda -->
    <item
        android:id="@+id/busqueda"
        android:title="@string/busqueda"
        android:icon="@drawable/ic_search_24"
        android:iconTint="@android:color/white"
        app:showAsAction="ifRoom|collapseActionView"
        app:actionViewClass="androidx.appcompat.widget.SearchView"/>

    <!-- Lista -->
    <item
        android:id="@+id/lista"
        android:title="@string/lista"
        android:icon="@drawable/outline_article_24"
        android:iconTint="@android:color/white"
        app:showAsAction="ifRoom"/>

    <!-- Campana (muestra snackbar) -->
    <item
        android:id="@+id/alerta"
        android:title="@string/alerta"
        android:icon="@drawable/outline_circle_notifications_24"
        android:iconTint="@android:color/black"
        app:showAsAction="ifRoom"/>

    <!-- Usuario (abre diálogo) -->
    <item
        android:id="@+id/infoUser"
        android:title="@string/infoUsuario"
        android:icon="@drawable/outline_account_box_24"
        android:iconTint="@android:color/black"
        app:showAsAction="ifRoom"/>
</menu>
