<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.VISOR_PRUEBA3" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.VISOR_PRUEBA3" parent="Base.Theme.VISOR_PRUEBA3" />

    <style name="ThemeOverlay.VISOR.ToolbarPopup" parent="ThemeOverlay.Material3.Light">
        <!-- Fondo del popup -->
        <item name="colorSurface">@android:color/white</item>
        <!-- Color del texto/íconos dentro del popup -->
        <item name="colorOnSurface">@android:color/black</item>
        <!-- Asegura texto negro (para compatibilidad) -->
        <item name="android:textColor">@android:color/black</item>
    </style>

</resources>