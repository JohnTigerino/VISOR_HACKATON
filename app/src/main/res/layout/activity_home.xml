<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Header / AppBar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="0dp"
        android:layout_height="56dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:logo="@drawable/frame_24_removebg_preview"
    app:menu="@menu/menu_home"                  
    android:background="@drawable/bg_register"/>

    <!-- Columna scroll -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/scroll"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <!-- Guidelines para márgenes laterales -->
            <androidx.constraintlayout.widget.Guideline
                android:id="@+id/gStart"
                android:orientation="vertical"
                app:layout_constraintGuide_percent="0.08"/>
            <androidx.constraintlayout.widget.Guideline
                android:id="@+id/gEnd"
                android:orientation="vertical"
                app:layout_constraintGuide_percent="0.92"/>

            <!-- Título con icono ubicación -->
            <TextView
                android:id="@+id/txtTitulo"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="Mi terreno  "
                android:textSize="22sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="@id/gStart"
                app:layout_constraintEnd_toEndOf="@id/gEnd"
                app:layout_constraintTop_toTopOf="parent"
                android:layout_marginTop="@dimen/space_16"/>

            <ImageView
                android:id="@+id/icUbicacion"
                android:layout_width="18dp"
                android:layout_height="18dp"
                app:srcCompat="@drawable/ic_location_on_24"
                app:tint="@color/teal_700"
                app:layout_constraintBottom_toBottomOf="@id/txtTitulo"
                app:layout_constraintStart_toEndOf="@id/txtTitulo"/>

            <!-- Botones: Dirección / Subir imagen -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDireccion"
                style="@style/Widget.Material3.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:text="Dirección"
                app:icon="@drawable/ic_search_24"
                app:iconPadding="8dp"
                app:layout_constraintStart_toStartOf="@id/gStart"
                app:layout_constraintEnd_toStartOf="@id/btnSubir"
                app:layout_constraintTop_toBottomOf="@id/txtTitulo"
                android:layout_marginTop="@dimen/space_12"
                android:layout_marginEnd="@dimen/space_8"/>

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnSubir"
                style="@style/Widget.Material3.Button.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:text="Subir imagen"
                app:icon="@drawable/ic_photo_camera_24"
                app:iconPadding="8dp"
                app:layout_constraintStart_toEndOf="@id/btnDireccion"
                app:layout_constraintEnd_toEndOf="@id/gEnd"
                app:layout_constraintTop_toTopOf="@id/btnDireccion"
                android:layout_marginStart="@dimen/space_8"/>

            <!-- Mapa / imagen dentro de tarjeta redondeada -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/cardMapa"
                android:layout_width="0dp"
                android:layout_height="0dp"
                app:layout_constraintStart_toStartOf="@id/gStart"
                app:layout_constraintEnd_toEndOf="@id/gEnd"
                app:layout_constraintTop_toBottomOf="@id/btnDireccion"
                android:layout_marginTop="@dimen/space_16"
                app:cardCornerRadius="12dp"
                app:cardElevation="2dp">

                <ImageView
                    android:id="@+id/imgMapa"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:adjustViewBounds="true"
                    android:scaleType="centerCrop"
                    app:layout_constraintDimensionRatio="16:9"
                    android:src="@drawable/placeholder_mapa"/>
            </com.google.android.material.card.MaterialCardView>

            <!-- Subtítulo y texto -->
            <TextView
                android:id="@+id/txtSubtitulo"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="Selección del área"
                android:textSize="20sp"
                android:textStyle="bold"
                app:layout_constraintStart_toStartOf="@id/gStart"
                app:layout_constraintEnd_toEndOf="@id/gEnd"
                app:layout_constraintTop_toBottomOf="@id/cardMapa"
                android:layout_marginTop="@dimen/space_16"/>

            <TextView
                android:id="@+id/txtDescripcion"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="Escribe la dirección o sube una imagen del terreno que deseas analizar"
                android:textColor="@color/textSecondary"
                android:lineSpacingExtra="2dp"
                app:layout_constraintStart_toStartOf="@id/gStart"
                app:layout_constraintEnd_toEndOf="@id/gEnd"
                app:layout_constraintTop_toBottomOf="@id/txtSubtitulo"
                android:layout_marginTop="@dimen/space_8"
                android:layout_marginBottom="@dimen/space_24"/>

        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
